<Window x:Class="RustRBLootEditor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RustRBLootEditor" xmlns:converters="clr-namespace:RustRBLootEditor.Converters"
        mc:Ignorable="d"
        Title="RB Loot Editor" Height="600" Width="1080" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <converters:UriFromShortnameConverter x:Key="UriFromShortnameConverter"/>
        <converters:FormatNumberConverter x:Key="FormatNumberConverter"/>
        <Style TargetType="{x:Type ListBoxItem}" x:Key="inventoryItemStyle">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Focusable" Value="False" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Margin" Value="0" />
            <Setter Property="Padding" Value="0" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="White" />
                    <Setter Property="BorderBrush" Value="White" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="{x:Type Grid}" x:Key="inventoryItemGridStyle">
            <Setter Property="Background" Value="#33BDBDBD" />
            <Setter Property="Width" Value="75" />
            <Setter Property="Height" Value="75" />
            <Setter Property="Margin" Value="2" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=IsMouseOver}" Value="True">
                    <Setter Property="Background" Value="#33BDBDBD" />
                    <Setter Property="Margin" Value="1"/>
                    <Setter Property="Width" Value="77" />
                    <Setter Property="Height" Value="77" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="ItemsGroupItem" TargetType="{x:Type GroupItem}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Expander Padding="0" BorderThickness="0" Header="{Binding Name}" IsExpanded="True" Margin="0,0,0,20">
                            <Expander.Style>
                                <Style TargetType="Expander">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Expander" x:Name="exp">
                                                <Grid>
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Name="ContentRow" Height="0"/>
                                                    </Grid.RowDefinitions>
                                                    <Border Name="border" Grid.Row="0" Background="{TemplateBinding Background}" BorderThickness="0" CornerRadius="0" >
                                                        <Grid Background="#33BDBDBD">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="*" />
                                                                <ColumnDefinition Width="0" />
                                                            </Grid.ColumnDefinitions>
                                                            <!--<ToggleButton x:Name="tb" FontFamily="Marlett" FontSize="9.75" Height="5" Grid.Column="1" 
                                                                                                  IsChecked="{Binding Path=IsExpanded,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}" />-->
                                                            <ContentPresenter x:Name="HeaderContent" Grid.Column="0" Margin="0" ContentSource="Header" RecognizesAccessKey="True" />
                                                        </Grid>
                                                    </Border>
                                                    <Border x:Name="Content" Grid.Row="1" BorderThickness="0" CornerRadius="0" >
                                                        <ContentPresenter Margin="0,4" />
                                                    </Border>
                                                </Grid>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsExpanded" Value="True">
                                                        <Setter TargetName="ContentRow" Property="Height" Value="{Binding ElementName=Content,Path=Height}" />
                                                        <!--<Setter Property="Content" TargetName="tb" Value="t"></Setter>-->
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Expander.Style>
                            <Expander.HeaderTemplate>
                                <DataTemplate>
                                    <Grid  Margin="0,0,0,3" HorizontalAlignment="{Binding Path=HorizontalAlignment, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ContentPresenter}}, Mode=OneWayToSource}" >
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition   />
                                            <ColumnDefinition  Width="Auto"/>
                                            <ColumnDefinition  Width="64"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock FontWeight="Bold" Margin="10,7,5,2" FontSize="14" Text="{Binding Converter={StaticResource CaseConverterToUpper}}" Foreground="White" />
                                    </Grid>
                                </DataTemplate>
                            </Expander.HeaderTemplate>
                            <ItemsPresenter/>
                        </Expander>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Storyboard x:Key="loadingSB" RepeatBehavior="1x" Completed="Storyboard_Completed">
            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="arcParent" Storyboard.TargetProperty="Visibility">
                <DiscreteObjectKeyFrame KeyTime="0:0:0" Value="{x:Static Visibility.Visible}"/>
            </ObjectAnimationUsingKeyFrames>
            <PointAnimationUsingPath Storyboard.TargetName="arc" Storyboard.TargetProperty="Point" Duration="0:0:0.2">
                <PointAnimationUsingPath.PathGeometry>
                    <PathGeometry>
                        <PathFigure StartPoint="200 100">
                            <ArcSegment Size="100 100" Point="200 300" SweepDirection="Counterclockwise" />
                            <ArcSegment Size="100 100" Point="200 100" SweepDirection="Counterclockwise" />
                        </PathFigure>
                    </PathGeometry>
                </PointAnimationUsingPath.PathGeometry>
            </PointAnimationUsingPath>
            <BooleanAnimationUsingKeyFrames Storyboard.TargetName="arc" Storyboard.TargetProperty="IsLargeArc">
                <DiscreteBooleanKeyFrame KeyTime="0:0:0.1" Value="True" />
                <DiscreteBooleanKeyFrame KeyTime="0:0:0.2" Value="False" />
            </BooleanAnimationUsingKeyFrames>
            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="arcParent" Storyboard.TargetProperty="Visibility">
                <DiscreteObjectKeyFrame KeyTime="0:0:0.2" Value="{x:Static Visibility.Collapsed}"/>
            </ObjectAnimationUsingKeyFrames>
        </Storyboard>
        <CollectionViewSource x:Key="ListBoxItems" Source="{Binding AllItems.Items}">
            <CollectionViewSource.GroupDescriptions>
                <PropertyGroupDescription PropertyName="category" />
            </CollectionViewSource.GroupDescriptions>
        </CollectionViewSource>
        <CollectionViewSource x:Key="LootTableListBoxItems" Source="{Binding LootTableFile.LootItems}">
            <CollectionViewSource.GroupDescriptions>
                <PropertyGroupDescription PropertyName="category" />
            </CollectionViewSource.GroupDescriptions>
        </CollectionViewSource>
    </Window.Resources>
    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="/RustRBLootEditor;component/Assets/rust-bg.jpg" Stretch="UniformToFill"/>
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid Background="#66000000">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="{Binding Activity}" Margin="10" Foreground="White" VerticalAlignment="Center"></TextBlock>
            <TextBox x:Name="FilePathTB" Visibility="Collapsed" Text="D:\Rust Server\Current\oxide\config\Skins.json" VerticalAlignment="Center" Margin="10,10,0,10"/>
            <StackPanel Orientation="Horizontal" Grid.Column="1">
                <Button x:Name="ImportFile" Grid.Column="2" Content="Import" HorizontalAlignment="Right" Margin="0,10,10,10" Padding="10,5" Click="ImportFile_Click"/>
                <Button x:Name="ExportFile" Grid.Column="2" Content="Export" HorizontalAlignment="Right" Margin="0,10,10,10" Padding="10,5" Click="ExportFile_Click"/>
            </StackPanel>
        </Grid>
        <Grid Grid.Row="1" Background="#4C222222">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="GAME ITEMS" Margin="10,10,10,0" FontWeight="Bold" Foreground="White" FontFamily="Lato Bold"/>
            <TextBlock Grid.Column="2" Margin="10,10,10,0" FontWeight="Bold" Foreground="White" FontFamily="Lato Bold">
                LOOT TABLE <Run Text="{Binding Filename}" Foreground="#9bcf3e"/>
            </TextBlock>
        </Grid>
        <Grid Grid.Row="2" Background="#4C222222">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid>
                <ListBox x:Name="AllItemsListbox" ItemsSource="{Binding AllItemsCollectionView}" ItemContainerStyle="{StaticResource inventoryItemStyle}"
                     SelectionMode="Extended" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"
                     Margin="10" ScrollViewer.HorizontalScrollBarVisibility="Disabled" Background="{x:Null}" BorderThickness="3" BorderBrush="#33000000">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel IsItemsHost="True" />
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.GroupStyle>
                        <GroupStyle ContainerStyle="{StaticResource ItemsGroupItem}">
                            <GroupStyle.Panel>
                                <ItemsPanelTemplate>
                                    <VirtualizingStackPanel Orientation="Vertical" />
                                </ItemsPanelTemplate>
                            </GroupStyle.Panel>
                        </GroupStyle>
                    </ListBox.GroupStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Style="{StaticResource inventoryItemGridStyle}" ToolTip="{Binding shortName}" MouseRightButtonUp="AllItemsGrid_MouseRightButtonUp">
                                <Grid.Triggers>
                                    <EventTrigger RoutedEvent="MouseRightButtonUp">
                                        <BeginStoryboard Storyboard="{StaticResource loadingSB}"/>
                                    </EventTrigger>
                                </Grid.Triggers>
                                <!--<Image Source="{Binding image}"  HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="5"></Image>-->
                                <TextBlock Margin="0,0,5,5" Text="{Binding displayName}" Foreground="White" FontWeight="Bold"
                                       TextWrapping="Wrap" TextAlignment="Center" VerticalAlignment="Bottom" HorizontalAlignment="Right"></TextBlock>

                                <Viewbox Width="30" Height="30">
                                    <Grid Width="200" Height="200">
                                        <Path Stroke="#9bcf3e" x:Name="arcParent" StrokeThickness="3" Fill="#9bcf3e" Visibility="Collapsed" Margin="-100,0,0,-100" VerticalAlignment="Bottom" HorizontalAlignment="Left" RenderTransformOrigin="0.5,0.5">
                                            <Path.RenderTransform>
                                                <TransformGroup>
                                                    <ScaleTransform ScaleY="-1"/>
                                                    <SkewTransform/>
                                                    <RotateTransform/>
                                                    <TranslateTransform/>
                                                </TransformGroup>
                                            </Path.RenderTransform>
                                            <Path.Data>
                                                <PathGeometry>
                                                    <PathFigure StartPoint="200 200" IsClosed="True">
                                                        <LineSegment Point="200 100" />
                                                        <ArcSegment x:Name="arc" Point="0 0" Size="100 100" SweepDirection="Counterclockwise">
                                                        </ArcSegment>
                                                    </PathFigure>
                                                </PathGeometry>
                                            </Path.Data>
                                        </Path>
                                    </Grid>
                                </Viewbox>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
            <Grid Grid.Column="2">
                <ListBox x:Name="LootTableItemsListbox" ItemsSource="{Binding Source={StaticResource LootTableListBoxItems}}" ItemContainerStyle="{StaticResource inventoryItemStyle}"
                     SelectionMode="Extended" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"
                     Margin="10" ScrollViewer.HorizontalScrollBarVisibility="Disabled" Background="{x:Null}" BorderThickness="3" BorderBrush="#33000000">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel IsItemsHost="True" />
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.GroupStyle>
                        <GroupStyle ContainerStyle="{StaticResource ItemsGroupItem}">
                            <GroupStyle.Panel>
                                <ItemsPanelTemplate>
                                    <VirtualizingStackPanel Orientation="Vertical" />
                                </ItemsPanelTemplate>
                            </GroupStyle.Panel>
                        </GroupStyle>
                    </ListBox.GroupStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Style="{StaticResource inventoryItemGridStyle}" ToolTip="{Binding displayName}" MouseRightButtonUp="LootTableGrid_MouseRightButtonUp">
                                <Grid.Triggers>
                                    <EventTrigger RoutedEvent="MouseRightButtonUp">
                                        <BeginStoryboard Storyboard="{StaticResource loadingSB}"/>
                                    </EventTrigger>
                                </Grid.Triggers>
                                <!--<Image Source="{Binding shortname, Converter={StaticResource UriFromShortnameConverter}}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="5"></Image>-->
                                <TextBlock Margin="0,0,5,5" ToolTip="{Binding Path=shortName}" Foreground="White" FontWeight="Bold"
                                       TextWrapping="Wrap" TextAlignment="Center" VerticalAlignment="Bottom" HorizontalAlignment="Right">
                                    x<Run Text="{Binding amountMin, Converter={StaticResource FormatNumberConverter}}"/>-<Run Text="{Binding amount, Converter={StaticResource FormatNumberConverter}}"/>
                                </TextBlock>
                                <Viewbox Width="30" Height="30">
                                    <Grid Width="200" Height="200">
                                        <Path Stroke="#9bcf3e" x:Name="arcParent" StrokeThickness="3" Fill="#9bcf3e" Visibility="Collapsed" Margin="-100,0,0,-100" VerticalAlignment="Bottom" HorizontalAlignment="Left" RenderTransformOrigin="0.5,0.5">
                                            <Path.RenderTransform>
                                                <TransformGroup>
                                                    <ScaleTransform ScaleY="-1"/>
                                                    <SkewTransform/>
                                                    <RotateTransform/>
                                                    <TranslateTransform/>
                                                </TransformGroup>
                                            </Path.RenderTransform>
                                            <Path.Data>
                                                <PathGeometry>
                                                    <PathFigure StartPoint="200 200" IsClosed="True">
                                                        <LineSegment Point="200 100" />
                                                        <ArcSegment x:Name="arc" Point="0 0" Size="100 100" SweepDirection="Counterclockwise">
                                                        </ArcSegment>
                                                    </PathFigure>
                                                </PathGeometry>
                                            </Path.Data>
                                        </Path>
                                    </Grid>
                                </Viewbox>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Grid>
    </Grid>
</Window>
